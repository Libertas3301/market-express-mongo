<form action="/filterRequest" method="POST">
    <label>costea geniu<input type="radio" name="radioOption" value="radioOption1"></label>
    <label>costea daun</label><input type="radio" name="radioOption" value="radioOption2"></label>
    <input type="submit" value="apasa">
</form>
<form action="/createpage">
    {{# each products }}
    <div class="row">
        {{# each this }}
        <p name="identificator">{{ this._id }}</p>
        <div class="col-sm-6 col-md-4">
            <div class="thumbnail">
                <img src="/uploads/{{this.imagePath}}" alt="">
                <div class="caption">
                    <h3>{{ this.title }}</h3>
                    <p class="description">{{ this.description }}</p>
                    <div class="clearfix">
                        <div class="price pull-left">{{ this.price }}</div>
                        <a class="btn btn-success pull-right" role="button">Add to shopping cart</a>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
    {{/each}}
</form>